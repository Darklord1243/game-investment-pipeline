# Archived tests

These tests targeted legacy root-level CSV miners (`twitch_miner.py`, `youtube_data_miner.py`, etc.) now under `archive/scripts/`. Active tests live in `tests/` and target `src/data_collection/`.
